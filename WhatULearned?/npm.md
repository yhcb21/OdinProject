- package manager

- package.json: a json file containing innfo about our project. such as its name or any dependency and their version number.
  - when you run `npm install` :
    - npm will read package.json file and see what it needs to install
    - any package we install is called dependencies 
    - any package we install is used during development phase we call them development dependencies

