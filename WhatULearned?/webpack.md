- Bundling:
  - we provide an entry point to Bundler 
  - it buiilds a dependency graph from that file, combines all relevant files tofether 
  - outputs a single file with all the necessary code included

- Webpack:
  - most popular js Bundler
  - --save-dev : record packages as development dependencies 
  - with webpacks somefile extensions are optional, like .js

- src:
  - where we keep all of out websites source code

- dist:
  - when we run webpack to bundle our code, it will bundle the files in dict/ directory.

- Building Js
  - 
